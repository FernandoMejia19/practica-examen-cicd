apiVersion: v1
kind: Service

metadata:
  name: inventario-blue-green

spec:
  type: NodePort

  selector:
    app: inventario-app
    color: green

  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000